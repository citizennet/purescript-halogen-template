{
  "name": "purescript-halogen-template",
  "version": "0.0.1",
  "repository": "git@github.com:citizennet/purescript-halogen-template",
  "author": "Thomas R. Honeyman <admin@thomashoneyman.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "postinstall": "bower i --silent",
    "clean": "rm -rf bower_components node_modules output",
    "build": "pulp --then 'parcel build app/index.html' build -I app",
    "watch": "pulp --then 'parcel --no-hmr app/index.html' build -I app"
  },
  "devDependencies": {
    "parcel-bundler": "^1.8.1",
    "pulp": "^12.2.0",
    "purescript": "^0.11.7"
  }
}
